fastapi==0.125.0
uvicorn[standard]==0.38.0
pydantic-settings==2.12.0
sqlalchemy==2.0.45
psycopg[binary]==3.3.2
python-multipart==0.0.20
presidio-analyzer==2.2.360
presidio-anonymizer==2.2.360
gliner==0.2.24
pypdf==6.4.1
weasyprint==63.1
openpyxl==3.1.5
xlrd==2.0.2
alembic==1.14.0
jinja2==3.1.4
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
# Foto-Doku (Phase 12.x) — EXIF-Extraktion (Aufnahmezeit, GPS) bei Foto-Uploads.
# Wird tolerant verwendet: fehlt Pillow, schlaegt die EXIF-Extraktion graceful
# fehl, der Upload selbst bleibt funktional.
Pillow>=10.0
# Sprachnotiz-Transkription (Phase 13.2) — beide Provider optional;
# Tests skippen, wenn die jeweilige Lib nicht installiert ist.
faster-whisper>=1.0
openai>=1.0
# Web-Push Benachrichtigungen (Phase 14.4). Bringt py_vapid + http_ece +
# cryptography mit. Wird im Produktionsbetrieb genutzt, im Test-Set
# wird der eigentliche Versand gemockt.
pywebpush>=1.14
